# talon bundle layer: composed on top of @deepseek-ai/dsh-base by the dsh
# profile loader (dsh.profile.bundles, appended after dsh-base by `dsh plugin
# ... add`). Row ids are loader labels (distinct from ctx service keys); row
# names are plugin module specifiers, resolved the same way an `import`
# would resolve them from the profile's node_modules — subpath names (e.g.
# 'talon-ui/boot') work exactly like '@deepseek-ai/dsh-tui/prompt' did for
# the old TUI, since both are ordinary package `exports` entries.
#
# session-projection-cache is not part of dsh-base (verified: absent from
# packages/bundle/base/cordis.patch.yml, present only in the web-app bundle)
# so talon inserts it itself for the cheap session-title ladder (spec §3.6).
# Its Config schema has no defaults (both fields z.natural().required()), so
# the row must state them explicitly; values mirror the web-app bundle's own
# tuned row (packages/bundle/web-app/cordis.patch.yml).
- insert:
    - id: storage
      name: '@deepseek-ai/dsh-storage'

    - id: storage-json
      name: '@deepseek-ai/dsh-storage-json'
      config:
        root: !!js dshHomePath('storages')

    - id: storage-domain
      name: '@deepseek-ai/dsh-storage-domain'
      config:
        backend: json

    - id: session-projection-cache
      name: '@deepseek-ai/dsh-session-projection-cache'
      config:
        writeEveryEvents: 200
        writeIntervalMs: 5000

    # dsh-base composes the user-questions SERVICE but not the model-facing
    # ask_user_question tool (verified: no tool-ask-user row in
    # packages/bundle/base/cordis.patch.yml; only plan-mode consumes the seam
    # there). UI-carrying compositions add the tool themselves, so talon —
    # which ships the question panel (spec §4.4, Tasks 9–11) — composes it
    # here. Found live in the T2 final acceptance run: without this row the
    # model cannot ask, and the multi-select acceptance line is unreachable.
    - id: tool-ask-user
      name: '@deepseek-ai/dsh-tool-ask-user'

    # sessionId pairing law (T2 carryover 2): set `sessionId` on BOTH rows or
    # NEITHER. talon-boot mints a fresh UUID when unset and talon-ui binds the
    # first root agent, so the defaults compose — but a single-sided pin binds
    # nothing (a pinned boot the UI ignores, or a UI waiting for an id no boot
    # creates).
    - id: talon-boot
      name: 'talon-ui/boot'

    - id: talon
      name: 'talon-ui'
